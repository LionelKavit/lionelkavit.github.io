## Context

Verified by frame extraction (July 17): t=0 shows pool default 20 / Balanced / 2 of 31 picks; t=93
shows pool **50**, Balanced, "✓ Bracket complete — 31/31", the CSV export click with
`my-bracket (6).csv · Done` in the download shelf, and the Analyst chat verdict naming the
ARG/FRA/ESP/ENG backbone "well-supported" with "2 for 2 on decided matches, 29 picks still alive"
(dates the recording to the opening days of the R32, late June). Git: the video entered the repo in
commit `3827b52` dated July 1 and was deployed to the live site no later than the July 8 pages build
— before the quarterfinals (July 9+). Divergence: video QFs show France–Canada and
Argentina–Algeria; the graded CSV (save #7) has France–Netherlands and Argentina–Colombia, and the
in-video Analyst references a Ghana R32 pick the graded bracket doesn't carry.

## Goals / Non-Goals

**Goals:** the demo beat reads as evidence, not product tour; the provenance note gains the
verifiable timing claim; the divergence is disclosed in Kavit's honest voice; no claim exceeds the
tape or the public record.

**Non-Goals:** no video edits/compression, no structure changes, no change to the no-pre-tournament-
lock stance, no timestamps cited that weren't verified (no "at 1:20" style references).

## Decisions

- **Demo lead-in paragraph (draft, tuned in place):** "This isn't a staged demo — it's the real
  late-June session behind the bracket graded below, recorded in the opening days of the knockouts:
  balanced risk, a 50-person pool, thirty-one picks, lock, export. And the Analyst says it on
  camera — Argentina, France, Spain, and England deep into the tournament, 'well-supported by the
  model' — before a single quarterfinal was played."
- **Caption:** `The real session — balanced risk, pool of 50, locked and exported` with span
  `▶ 1:38`. Rationale: the caption's job flips from feature list to evidence label; duration replaces
  the generic "Demo" tag.
- **Provenance note:** keep attribution and no-lock stance verbatim in spirit; add two sentences:
  the session is on tape above and public (repo July 1, live before the QFs), and the graded export
  is one save later with a few pick changes (spanning R32 and R16). Rationale: the divergence disclosure *is* the
  credibility — it preempts the pause-and-compare reader who spots France–Canada in the video.
- **Quote handling:** the Analyst's words are quoted only in the fragment actually visible on
  camera ("well-supported by the model"); everything else paraphrased.

## Risks / Trade-offs

- **Reader confuses "public before QFs" with a pre-tournament lock claim** → the note states the
  no-lock stance in the same breath; the two claims are kept visibly distinct.
- **Divergence disclosure invites scrutiny of the video** → that's the point; every named divergence
  was verified by frame, and none touches the graded QF/SF record claims.
- **Copy length in the note** → capped at two added sentences; trim elsewhere if it crowds.
