## 1. Implement

- [x] 1.1 Remove the `.hero-pills` block from the hero; tune `.hero-sub` bottom margin if the hero looks hollow
- [x] 1.2 Add the "Built with" beat (label + pills, `.tech-stack` wrapper) between Ask the Analyst and the disclaimer, with a divider seam
- [x] 1.3 Add the light-surface pill variant (`.tech-stack .pill`) using page tokens

## 2. Verify

- [x] 2.1 Mobile (375px/320px): pills wrap centered, legible on cream, no overflow; hero reads clean without pills
- [x] 2.2 Desktop (≥1280px): hero balance and bottom-of-page rhythm check
