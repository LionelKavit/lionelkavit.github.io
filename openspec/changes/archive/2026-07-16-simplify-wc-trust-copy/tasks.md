## 1. Implement

- [x] 1.1 Rewrite the three pipeline node titles/descriptions in plain language (per design.md drafts)
- [x] 1.2 Replace the connector line + four `.arch-card` blocks with the single guardrail sentence
- [x] 1.3 Remove the orphaned `.arch-grid`/`.arch-card*` CSS rules and the `.arch-grid` mobile reference (leave the shared `code` selector rule intact)

## 2. Verify

- [x] 2.1 Skim test: no library/type/I-O jargon anywhere in the section
- [x] 2.2 Mobile (375px/320px) and desktop (≥1280px): diagram and closing line render cleanly, no overflow
